{
  "name": "the-foundry-library",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:backend": "npm --workspace apps/backend run dev",
    "dev:frontend": "npm --workspace apps/frontend run dev",
    "build": "npm -ws run build",
    "test": "npm -ws run test",
    "lint": "npm -ws run lint"
  }
}